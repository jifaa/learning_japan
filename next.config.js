/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js automatically uses src/ when it exists.
  // No special config needed — src/app/ is the app directory.
};

module.exports = nextConfig;
